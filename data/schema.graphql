# Represents a Movie
type Movie {
  id: ID
  title: String
  plot: String
  year: String
  runtime: Int
  genres: [String]
  director: String
  actors: String
  posterUrl: String
}

# Query any Movie by number or name
type Query {
  query: Query
  allMovies: [Movie]
}
